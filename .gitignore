# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-server
*.local

# Environment / secrets
.env
.env.*
!.env.example

# Drizzle
drizzle/meta

# Playwright (e2e) — regenerated on every run
e2e/test-results
e2e/playwright-report

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# k6 load-test artifacts — run outputs, not source
load-test/reports

# security-test artifacts — .state holds live session cookies for
# throwaway test identities (not secrets worth keeping, but not source
# either); raw/ holds per-category JSON that report.md/report.html already
# summarize
security-test/.state
security-test/reports/*/raw
